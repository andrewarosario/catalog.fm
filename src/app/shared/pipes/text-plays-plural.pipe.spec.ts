import { TextPlaysPluralPipe } from './text-plays-plural.pipe';

describe('TextPlaysPluralPipe', () => {
  it('create an instance', () => {
    const pipe = new TextPlaysPluralPipe();
    expect(pipe).toBeTruthy();
  });
});
